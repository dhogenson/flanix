[package]
name = "sync-cli"
version.workspace = true
edition.workspace = true

[dependencies]
sync-core = { path = "../sync-core" }
sync-tui = { path = "../sync-tui" }
tokio.workspace = true
anyhow.workspace = true
clap = { version = "4", features = ["derive"] }
uuid.workspace = true
