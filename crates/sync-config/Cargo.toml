[package]
name = "sync-config"
version.workspace = true
edition.workspace = true

[dependencies]
sync-errors = { path = "../sync-errors" }
serde.workspace = true
toml.workspace = true
directories = "6.0.0"

[lib]
name = "sync_config"
path = "src/lib.rs"
