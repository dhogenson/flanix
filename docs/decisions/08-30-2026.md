Starting to write out how the project is going and keeping my notes here

let me gather all the todos and keep them in the docs instead of all over the project

also moved unit tests from sync.rs to sync/tests.rs

# TODO

- [ ] this is a full rescan of folder which for big folders is bad
- [x] the db map is keyed by path so if a folder gets renamed/moved it looks like ts new and gets reuploaded under a new uuid the fix:
content hasing, using something really cheap to store in a database and if a file is renamed you can tell because they are the same hashes
- [x] the bucket is not consulted at all, the cloud state is really just "what the db things is in the cloud"
so that means that if an object is deleted from s3 but the db row is still there its treated as up to date
and skipped
- [ ] rewrite the tests what were made by AI
- [x] figure out what local constatin is and fix it (used ai to fix it for now)
- [x] sometimes a function in the sync-core crate just takes in a path to return something else, but more than one folder can have a file name so make it also require a namespace to
- [ ] fix the file paths, like in the db is "test_files/whatever.txt" but i just want root path like "whatevery.txt"
