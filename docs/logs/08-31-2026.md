1. Started working on the pull function
2. Made it so that when you push to the database it doesnt give the full path of the file on the local filesystem
3. I finished the pull funciton

so now i have to figure out how to make the damon
after that im going to clean up the sync core crate and make
it way better and cleaning, maybe

next i need to try and make a windows build, but im having touble with linking errors

i cant seem to fix the linking error so deepseek told me to use `xwin` so let me try that, if that works i will it add it the readme

ok that took awhile and i got it to work finally, just had to add some depenedces like llvm and clang.
use this command `cargo xwin build --release --target x86_64-pc-windows-msvc` to build for windows and install xwin if you have not by running this command:
`cargo install cargo-xwin`
ok that worked so im going to update the readme
